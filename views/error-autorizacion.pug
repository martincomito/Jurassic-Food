extends layout

block content
  .contenedor
    .encabezado
      h2 Acceso Denegado
    
    .error(style="text-align: center; margin-top: 20px;")
      p(style="font-size: 1.2em; font-weight: bold;") Vista no autorizada para el usuario
      
      div(style="margin-top: 30px;")
        a.boton.boton-principal(href="/") Volver al Inicio
